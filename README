
Basic upnp av deamon including server, renderer, and controller based on UPNP
Forum specs v 1.0

Alper Akcan.


- MIPS - uCLinux Configuration;

./autogen.sh
ac_cv_func_malloc_0_nonnull=yes ../configure --host=mips-linux-uclibc --disable-controller --disable-shared
make -s

- simple run

# upnpd -d mediaserver -i eth0 -o directory=/path/to/share -v
# upnpd -d controller -i eth0 -v
