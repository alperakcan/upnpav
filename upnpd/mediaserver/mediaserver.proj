IF ENABLE_MEDIASERVER
LIB upnpd_mediaserver
{
  INCLUDE ../

  USE upnpd_common
  
  SOURCE mediaserver.c

  HEADER mediaserver.h
  
  LIBS(HAVE_LIBREADLINE) readline
  LIBS(HAVE_LIBREADLINE) termcap
}
ENDIF
